---
export interface Props {
    id: string;
    class: string;
}

const { id, class: className } = Astro.props;
---

<!-- 
<section id=`${id}` class=`w-full h-full container absolute ${className}`>
    <div
        class="main-slot absolute w-full h-full flex justify-center items-center"
    >
        <slot />
    </div>
    <div
        class="copy-slot absolute w-full h-full flex justify-center items-center"
    >
        <slot />
    </div>
</section> -->
<div
    id=`${id}`
    class=`main-slot absolute w-full h-full flex justify-center items-center ${className}`
>
    <slot />
</div>
<div
    id=`${id}-copy`
    class=`copy-slot absolute w-full h-full flex justify-center items-center ${className}`
>
    <slot />
</div>

<style>
    .main-slot,
    .copy-slot {
        top: 0;
        left: 0;
        transition: all 0.8s;
    }

    .main-slot {
        clip-path: polygon(0 0, 100% 0%, 0% 100%);
    }

    .copy-slot {
        clip-path: polygon(100% 100%, 100% 0%, 0% 100%);
    }

    .leave.main-slot {
        transform: translateX(-100vw) rotateY(90deg);
    }

    .leave.copy-slot {
        transform: translateX(200vw) rotateY(90deg);
    }

    /* .container.leave {
        transform: translateX(100);
    } */

    /* .rotate {
        transform: rotateY(90deg);
    } */
</style>
